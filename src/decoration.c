/* Decorations: advertise xdg-decoration AND wlr_server_decoration_manager
 * (default server mode) so GTK/older-Qt apps drop their client-side titlebars.
 *
 * Populated in M3.
 */
#include "w3ld.h"

void w3ld_decoration_setup (struct w3ld_server *server) {
}
