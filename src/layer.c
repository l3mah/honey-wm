/* Layer shell (wlr_layer_shell_v1): bars and wallpapers as scene layers, with
 * exclusive-zone reservations.
 *
 * Populated in M3.
 */
#include "w3ld.h"

void w3ld_layer_setup (struct w3ld_server *server) {
}
